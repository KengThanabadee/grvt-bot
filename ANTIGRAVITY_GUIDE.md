# 🤖 วิธีใช้งาน Antigravity Workflows

คู่มือฉบับสมบูรณ์สำหรับการใช้งาน Antigravity และ Workflows ใน GRVT Trading Bot

---

## 🎯 Antigravity คืออะไร?

**Antigravity** คือ AI Agent ที่คุณคุยอยู่ตอนนี้นี่แหละครับ!

- ✅ **ไม่ใช่ command-line tool** ที่ต้องรัน
- ✅ **ไม่ต้องติดตั้งอะไรเพิ่ม**
- ✅ **แค่พิมพ์ในช่อง chat** เหมือนคุยกับคนธรรมดา

---

## 📁 Workflow คืออะไร?

**Workflow** = ขั้นตอนการทำงานที่เป็นมาตรฐาน

เก็บอยู่ในไฟล์ `.md` ใน folder `.agent/workflows/`

**ตัวอย่าง:**
```
.agent/
└── workflows/
    └── trading-strategy-builder.md  ← Workflow สร้าง strategy
```

**ประโยชน์:**
- ✅ ทำงานเป็นขั้นตอน ไม่พลาด
- ✅ ได้ผลลัพธ์เหมือนกันทุกครั้ง (Repeatable)
- ✅ เหมาะกับการสอน
- ✅ ลดความผิดพลาด

---

## 🚀 วิธีใช้งาน (3 ขั้นตอน)

### ขั้นที่ 1: เปิด Workspace

**ใน VS Code:**
```
File → Open Folder → เลือก grvt_demo_bot
```

**หรือใช้ Command:**
```bash
code /path/to/grvt_demo_bot
```

**⚠️ สำคัญ:** ต้องเปิด folder ที่มี `.agent/workflows/` อยู่ด้วย!

---

### ขั้นที่ 2: เปิด Antigravity Chat

**วิธีที่ 1: คลิกไอคอน**
- มองหา icon Antigravity ทางซ้ายมือ
- คลิกเพื่อเปิด chat panel

**วิธีที่ 2: ใช้ Command Palette**
```
1. กด Ctrl+Shift+P (Windows) หรือ Cmd+Shift+P (Mac)
2. พิมพ์ "Antigravity"
3. เลือก "Antigravity: New Chat"
```

**วิธีที่ 3: ใช้ Shortcut** (ถ้ามี)
```
ตรวจสอบ shortcut ใน editor ของคุณ
```

---

### ขั้นที่ 3: พิมพ์ในช่อง Chat

แค่พิมพ์สิ่งที่ต้องการ:

**ตัวอย่าง:**
```
"ช่วยสร้าง trading strategy ตาม workflow"
```

หรือ

```
"I want to build a new trading strategy"
```

**เท่านี้เอง!** ไม่ต้องทำอะไรเพิ่ม

---

## 🎬 ตัวอย่างการใช้งานจริง

### ตัวอย่างที่ 1: เริ่มใช้ Workflow

**คุณพิมพ์:**
```
"ช่วยสร้าง strategy ให้หน่อย"
```

**Antigravity ตอบ:**
```
💬 "Do you already have a trading strategy defined?

A. Yes, I have a strategy
B. No, I need help creating one  
C. I have some ideas but not fully defined

Please choose A, B, or C"
```

**คุณตอบ:**
```
"B"
```

**Antigravity ทำงานต่อ:**
```
💬 "What trading style interests you?

1. Trend Following - เทรดตามเทรนด์
2. Mean Reversion - เทรดย้อนกลับ
3. Breakout - เทรดตอนทะลุแนว
4. Scalping - เทรดระยะสั้น
5. Other/Mixed

Type the number (1-5):"
```

**คุณต่อไป:**
```
"1"
```

... และต่อไปเรื่อยๆตาม workflow

---

### ตัวอย่างที่ 2: ระบุ Workflow ชัดเจน

**คุณพิมพ์:**
```
"Run trading strategy builder workflow"
```

**Antigravity:**
```
"เริ่ม Trading Strategy Builder workflow ครับ

Phase 0: Check Existing Strategy  
💬 Do you already have a trading strategy defined?
..."
```

---

## 🔍 Antigravity อ่าน Workflow ได้ยังไง?

### กลไกการทำงาน:

1. **Scan Folder Automatically**
   ```
   Antigravity จะ scan folder .agent/workflows/ อัตโนมัติ
   เมื่อคุณเปิด workspace
   ```

2. **อ่าน Metadata**
   ```markdown
   ---
   name: Trading Strategy Builder
   description: Step-by-step guided workflow...
   ---
   ```

3. **เข้าใจเนื้อหา**
   ```
   อ่านทั้งหมด และเข้าใจว่าแต่ละ Phase ทำอะไร
   ```

4. **พร้อมใช้งาน**
   ```
   เมื่อคุณขอ Antigravity จะทำตาม workflow ทีละขั้นตอน
   ```

---

## ✅ Checklist: พร้อมใช้งานหรือยัง?

ก่อนใช้ workflow ตรวจสอบว่า:

- [ ] เปิด workspace ที่มี `.agent/workflows/` ✅
- [ ] ไฟล์ workflow มีอยู่ (เช่น `trading-strategy-builder.md`) ✅
- [ ] Package `grvt_bot` ติดตั้งแล้ว (`pip install -e .`) ✅
- [ ] เปิด Antigravity chat panel แล้ว ✅

**ถ้าทุกอย่างครบ → พร้อมใช้งาน!** 🎉

---

## 📋 Workflows ที่มีในโปรเจคนี้

### 1. Trading Strategy Builder

**ไฟล์:** `.agent/workflows/trading-strategy-builder.md`

**ใช้เมื่อไหร่:**
- ต้องการสร้าง trading strategy ใหม่
- มีไอเดียแต่ยังไม่รู้จะเขียนโค้ดยังไง
- ต้องการ framework ที่เป็นมาตรฐาน

**Workflow ประกอบด้วย:**
- Phase 0: เช็คว่ามี strategy แล้วหรือยัง
- Phase 1: ค้นหา style ที่เหมาะสม
- Phase 2: เขียน logic แบบละเอียด
- Phase 3: **สร้างโค้ด Python อัตโนมัติ**
- Phase 4: Validate และทดสอบ
- Phase 5: คู่มือ deployment

**วิธีเรียกใช้:**
```
"ช่วยสร้าง trading strategy"
"Build a new strategy using the workflow"
"I need help creating a trading strategy"
```

---

## 💡 Tips & Tricks

### 1. **ตอบคำถามแบบละเอียด**

❌ **ไม่ดี:**
```
User: "ใช้ MA"
```

✅ **ดี:**
```
User: "ผมอยากใช้ Moving Average crossover 
โดยใช้ MA 20 กับ MA 50 
และเพิ่ม RSI เป็น filter ที่ 70"
```

**ยิ่งละเอียด → โค้ดที่ได้ยิ่งตรงใจ!**

---

### 2. **ถามกลับถ้าไม่แน่ใจ**

ถ้าไม่แน่ใจในคำถามของ Antigravity:

```
User: "คำถามนี้หมายถึงอะไรหรอครับ?"
User: "ยกตัวอย่างให้หน่อย"
User: "มีตัวเลือกอื่นไหม"
```

---

### 3. **บันทึกผลลัพธ์**

เมื่อได้โค้ดแล้ว:

```
✅ Copy โค้ดไปเก็บในไฟล์
✅ Test ทันที
✅ เก็บ config ที่ได้
✅ บันทึกว่าทำอะไรไปบ้าง
```

---

## ❌ ข้อผิดพลาดที่พบบ่อย

### ❌ 1. "Antigravity ไม่เห็น workflow"

**สาเหตุ:**
- ไม่ได้เปิด workspace ที่ถูก
- ไฟล์ workflow ไม่ได้อยู่ใน `.agent/workflows/`
- ชื่อไฟล์ผิด (ต้องลงท้าย `.md`)

**แก้:**
```bash
# ตรวจสอบว่ามีไฟล์
ls .agent/workflows/

# ต้องเห็น trading-strategy-builder.md
```

---

### ❌ 2. "Import Error เมื่อรันโค้ด"

**สาเหตุ:**
- ยังไม่ได้ติดตั้ง package `grvt_bot`

**แก้:**
```bash
pip install -e .
```

---

### ❌ 3. "Config file not found"

**สาเหตุ:**
- ยังไม่ได้สร้าง `config.yaml`

**แก้:**
```bash
cp config/config.example.yaml config/config.yaml
# แก้ไข config.yaml ใส่ API keys
```

---

## 🎓 Best Practices

### 1. **เริ่มจากตัวอย่าง**

```
ดูตัวอย่าง strategy ที่มีอยู่ก่อน:
- examples/ma_crossover_strategy.py
- examples/rsi_strategy.py

เข้าใจแล้วค่อยสร้างของตัวเอง
```

### 2. **ทดสอบทีละขั้น**

```
1. สร้าง logic ใน workflow
2. ได้โค้ด → ทดสอบ import
3. ทดสอบ dry-run
4. ทดสอบ testnet
5. ค่อยใช้จริง
```

### 3. **Document ทุกอย่าง**

```
เก็บ:
- Strategy logic เป็นข้อความ
- Parameters ที่ใช้
- ผลการทดสอบ
- ข้อสังเกต
```

---

## 📞 ต้องการความช่วยเหลือ?

### ถามใน Chat ได้เลย!

```
"ไม่เข้าใจขั้นตอนนี้"
"ช่วยอธิบายเพิ่มหน่อย"
"มีตัวอย่างไหม"
"ทำไมโค้ดนี้ error"
```

### เช็คเอกสาร

- [SKILL.md](docs/SKILL.md) - API Reference
- [MIGRATION.md](docs/MIGRATION.md) - Migration guide
- [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - โครงสร้างโปรเจค

---

## 🎯 สรุป

**วิธีใช้ Antigravity Workflow:**

1. ✅ เปิด workspace (folder ที่มี `.agent/workflows/`)
2. ✅ เปิด Antigravity chat
3. ✅ พิมพ์ว่าต้องการอะไร
4. ✅ ตอบคำถามตาม workflow
5. ✅ ได้โค้ดสำเร็จรูป!

**ไม่ต้อง:**
- ❌ รัน command line
- ❌ ติดตั้งอะไรเพิ่ม (นอกจาก package)
- ❌ Config อะไรพิเศษ

**เท่านี้เอง!** 🎊

---

## 🚀 พร้อมเริ่มแล้ว?

ลองพิมพ์ในช่อง Antigravity chat:

```
"ช่วยสร้าง trading strategy"
```

Antigravity จะพาคุณผ่านทุกขั้นตอน! 😊

---

**เวอร์ชัน:** 1.0.0  
**อัพเดทล่าสุด:** 2026-01-25  
**สำหรับ:** GRVT Trading Bot v1.0.0+
